import { defineComponentConfig } from '@mixte/snippets/low-code-editor/utils';

export const config = defineComponentConfig({
  name: 'LayoutDiv',
  displayName: '容器',
  group: 'basic',
  config: {

  },
});
